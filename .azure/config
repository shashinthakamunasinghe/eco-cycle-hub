# .azure/config
[defaults]
group = eco-cycle-hub-rg
sku = P1v2
appserviceplan = eco-cycle-hub-asp
location = westus2
web = eco-cycle-hub

[webapp]
startup_file = server.js
build_args = --env-file=.env.local
node_env = production
